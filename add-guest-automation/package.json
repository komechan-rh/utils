{
  "name": "add-guest-automation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build && cp appsscript.json dist/appsscript.json",
    "watch": "tsc --watch",
    "push": "pnpm build && clasp push",
    "deploy": "clasp deploy",
    "open": "clasp open-script",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome lint src",
    "format": "biome format --write src",
    "check": "biome check --write src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/google-apps-script": "^1.0.7",
    "clasp": "^1.0.0",
    "typescript": "^7.0.2",
    "vite": "^7.3.0",
    "vitest": "^4.0.18"
  }
}
