{
  "name": "manager-line-notifications",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "build": "vite build && cp appsscript.json dist/appsscript.json",
    "watch": "tsc --watch",
    "version:bump": "pnpm version patch --no-git-tag-version",
    "push": "pnpm run version:bump && pnpm build && clasp push -u ${CLASP_USER:?CLASP_USER is not set} --force",
    "deploy": "pnpm run version:bump && clasp deploy -u ${CLASP_USER:?CLASP_USER is not set}",
    "redeploy": "pnpm run version:bump && pnpm build && clasp push -u ${CLASP_USER:?CLASP_USER is not set} --force && clasp redeploy -u ${CLASP_USER:?CLASP_USER is not set} ${DEPLOYMENT_ID:?DEPLOYMENT_ID is not set}",
    "deploy:webhook": "infisical run -- bash scripts/deploy-and-update-webhook.sh",
    "open": "clasp open-script -u ${CLASP_USER:?CLASP_USER is not set}",
    "sync-props": "infisical run -- node scripts/sync-script-properties.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome lint src",
    "format": "biome format --write src",
    "check": "biome check --write src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/google-apps-script": "catalog:",
    "@google/clasp": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
