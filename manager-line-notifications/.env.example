# scripts/deploy-and-update-webhook.sh が使用する環境変数のサンプル。
# コピーして .env を作成し、実際の値を設定してください（.env はコミットしないこと）。

# clasp の複数アカウント識別子（clasp login --user <name> で作成したもの）
CLASP_USER=your-clasp-user

# LINE Developers で発行したチャネルアクセストークン（長期）
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
